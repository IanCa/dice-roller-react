{
  "name": "dice-roller",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist -f"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.158.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.3.3"
  }
}
